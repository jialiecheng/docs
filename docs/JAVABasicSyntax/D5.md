# 05.02_数组的初始化动态初始化）（掌握）
## 05.02_数组的初始化动态初始化）（掌握）
#### A：什么是数组的初始化
就是为数组开辟连续的内存空间，并为每个数组元素赋值
#### B：如何对数组进行初始化
```java
a:动态初始化 ---只指定长度，由系统给出初始化值

整数类型 byte,short,int,long 默认初始化值为 0
浮点类型 double,float 默认初始化值为 0.0
布尔类型 boolean 默认初始化值为 false
字符类型默认初始化值为 ’\u0000’ char 在内存中占两个字节是16个二进制位
‘\u0000’，每一个0其实代表的是16进制的0，那么四个0就是代表16个二进制位
int[] arr = new int[5];
 
b:静态初始化 给出初始化值，由系统决定长度 
```
#### C：动态初始化的格式：
- 数据类型[] 数组名 = new 数据类型[数组长度]
- int[] i = new int[2]
## 05.03_Java中的内存分配以及栈和堆的区别）
#### A：栈（掌握）
- 栈：先进后出
- 存储局部变量
#### B：堆（掌握）
- 存储new出来的数组或对象
#### C：方法区
- 面向对象部分详细了解
#### D：本地方法区
- 和系统相关
#### E：寄存器
- 给CPU使用
## 05.07_数组的初始化静态初始化）
#### A：静态初始化的格式
```java

数据类型[] 数组名 = new 数据类型[]{元素1，元素2，…}; //可以先声明后赋值

int[] arr; arr = new int[]{11,22,33};

简化格式： 数据类型[] 数组名 ={元素1，元素2，…}; //声明赋值必须在一起

不允许动态初始化和静态初始化结合 例如：int[] arr = new int[3]{11,22,33};

arr.length 代表数组的长度
```
05.08_数组操作的两个常见小问题越界和空指针）
#### A：静态初始化的格式
```java
ArrayIndexOutOfBoundsException：数组索引越界异常
原因：你访问了不存在的索引

NullPointerException：空指针异常
原因：数组已经不再指向堆内存了，而你还用数组名去访问元素

int arr = {1,2,3};
arr = null;
System.out.println(arr[0]);
```
## 05.14_二维数组概述和格式1的讲解）
#### A：二维数组概述
#### B：二维数组格式1
```java
int[][] arr = new int[3][2];
```
#### C:注意事项
```java
以下格式也可以表示二维数组
数据类型 数组名[][] = new 数据类型[m][n];
数据类型[] 数组名[] = new 数据类型[m][n];
```
#### D:注意以下定义的区别
```java
int[] x; //x是一维数组
int[] y[]; //y是二维数组
int[] x,y[]; //x是一维数组,y是二维数组
```
## 05.16_二维数组格式2的讲解）
#### A：二维数组格式2
```java
int[][] arr = new int[2][];
arr[0] = new int[3]; //第一个一维数组中可以存储三个int值
arr[1] = new int[4]; //第二个一维数组中可以存储四个int值
```
05.17_二维数组格式3的讲解）
#### A：二维数组格式3
```java
int[][] arr = {{1,2,3},{4,5},{6,7,8,9}};
每个大括号都代表一个一维数组
arr[1] = new int[4]; //第二个一维数组中可以存储四个int值
```
## 05.20_参数传递问题）
#### A：参数传递问题
- 基本数据类型的值传递，不改变原值，因为调用后就会弹栈，局部变量随之消失
- 引用数据类型的值传递，改变原值，因为即使方法弹栈，但是堆内存数组对象还在而且已经被改变，可以通过地址继续访问
- Java中到底是值传递还是引用传递呢？  ------Java中只有传值因为地址也是值
